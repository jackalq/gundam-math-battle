Object.assign(WarScene.prototype,{
  async banner(text,color){this.bannerEl.textContent=text;this.bannerEl.style.color=color;this.bannerEl.classList.remove('show');void this.bannerEl.offsetWidth;this.bannerEl.classList.add('show');this.log.textContent=text;await delay(650)},
  async performAttack(attacker,target,min,max,enemyShot){if(!WarRoles.Target.alive(attacker)||!WarRoles.Target.alive(target))return;const a=this.elFor(attacker),t=this.elFor(target);if(!a||!t)return;a.classList.add('firing');Sound.shot(enemyShot);await this.projectile(a,t,enemyShot);const dmg=WarRoles.Attacker.damage(target,min,max);this.log.textContent=`${attacker.name} → ${target.name}　-${dmg} HP`;t.classList.add('unit-hit');this.updateOne(target);await this.explode(t,target.hp<=0);await delay(190);a.classList.remove('firing');t.classList.remove('unit-hit');if(target.hp<=0){this.log.textContent=`${target.name} 被擊破！`;await delay(240)}},
  updateOne(u){const e=this.elFor(u);if(!e)return;const hp=e.querySelector('.unit-hp i'),n=e.querySelector('.unit-name span:last-child');hp.style.width=u.hp+'%';n.textContent=Math.ceil(u.hp);if(u.hp<=0)e.classList.add('destroyed')},
  projectile(from,to,enemyShot){return new Promise(resolve=>{const fr=this.field.getBoundingClientRect(),a=from.getBoundingClientRect(),b=to.getBoundingClientRect();const p=document.createElement('i');p.className='war-projectile'+(enemyShot?' enemy-shot':'');const sx=a.left+a.width/2-fr.left,sy=a.top+a.height*.53-fr.top,ex=b.left+b.width/2-fr.left,ey=b.top+b.height*.53-fr.top;p.style.left=sx+'px';p.style.top=sy+'px';this.field.appendChild(p);const anim=p.animate([{transform:'translate(0,0) scaleX(.7)',opacity:.2},{transform:`translate(${ex-sx}px,${ey-sy}px) scaleX(1.4)`,opacity:1}],{duration:280,easing:'ease-in'});anim.onfinish=()=>{p.remove();resolve()}})},
  explode(target,big=false){return new Promise(resolve=>{const fr=this.field.getBoundingClientRect(),r=target.getBoundingClientRect(),e=document.createElement('i');e.className='explosion';e.style.left=(r.left+r.width/2-fr.left-9)+'px';e.style.top=(r.top+r.height/2-fr.top-9)+'px';this.field.appendChild(e);const anim=e.animate([{transform:'scale(.2)',opacity:0},{transform:`scale(${big?3.3:1.8})`,opacity:1},{transform:`scale(${big?4.6:2.5})`,opacity:0}],{duration:big?520:330,easing:'ease-out'});anim.onfinish=()=>{e.remove();resolve()}})},
  pauseChanged(){if(game.paused)this.overlay.classList.remove('show');else if(game.phase==='question')this.overlay.classList.add('show')}
});
